[pytest]
# Run tests from the backend/ dir so `import app...` resolves without extra config.
testpaths = tests
# ``auto`` lets ``async def`` test functions be awaited without a decorator.
asyncio_mode = auto
# Silence the pytest-asyncio 1.x default-fixture-scope deprecation warning.
asyncio_default_fixture_loop_scope = function
